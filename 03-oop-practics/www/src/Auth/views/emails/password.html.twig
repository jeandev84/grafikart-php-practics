<p>
    Vous avez demande la reinitialisation de votre mot de passe.
</p>

<p>
    <a href="{{ domain }}{{ path('auth.password.reset', {id: id, token: token}) }}">
        {{ domain }}{{ path('auth.password.reset', {id: id, token: token}) }}
    </a>
</p>